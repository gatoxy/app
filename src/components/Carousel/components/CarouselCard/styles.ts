import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    
  },

  image: {
    width: 110,
    height: 160,
    borderRadius: 12,
  },
  
  image_small: {
    width: 85,
    height: 120,
    borderRadius: 12,
  },
});